# Copia este archivo a .env.revision.local y pon tus datos.
# .gitignore ya cubre `.env.*.local`, así que nunca se sube al repo.
# Es solo para el barrido de pantallas (scripts/revisar-pantallas.mjs).
INMERSIA_EMAIL=tu@correo.com
INMERSIA_PASSWORD=tu-clave
